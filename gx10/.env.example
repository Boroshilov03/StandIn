# Set to "true" for deterministic mock responses (demo-safe).
# Set to "false" to call local Ollama; deterministic regex fallback still applies.
GX10_MOCK=true

OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=gemma3:4b
OLLAMA_TIMEOUT_SECONDS=12

PORT=8001
